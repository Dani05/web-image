spring.application.name=web-image-backend

# Security Configuration
security.jwt.secret-key=

# File upload directory (override with env var APP_UPLOAD_DIR or mount a PVC here)
app.upload.dir=${user.home}/web-image-uploads
