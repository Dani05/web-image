services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: webimage
      POSTGRES_USER: webimage
      POSTGRES_PASSWORD: webimage
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:

